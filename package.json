{
  "name": "mcp-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Production-level MCP tools for resume portfolio",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
